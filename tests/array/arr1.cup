fn main() => i32 {
    let arr : [i32; 5] = [1, 2, 3, 4, 5];
    let sum : i32 = 0;
    for (let i : i32 = 0; i < arr.len; i = i + 1) {
        sum = sum + arr[i];
    }
    return sum; // expect 15
}